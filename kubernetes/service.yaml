apiVersion: v1
kind: Service
metadata:
  name: book-api-service
  labels:
    app: book-api
spec:
  type: NodePort
  selector:
    app: book-api
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort
